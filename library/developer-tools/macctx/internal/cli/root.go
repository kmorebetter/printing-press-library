// Copyright 2026 hiten-shah. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"
)

var version = "1.0.0"

// Execute runs the CLI.
func Execute() error {
	rootCmd := &cobra.Command{
		Use:          "macctx-pp-cli",
		Short:        "Give agents eyes, context, and guarded hands on your Mac",
		SilenceUsage: true,
		Version:      version,
	}
	rootCmd.SetVersionTemplate("macctx-pp-cli {{ .Version }}\n")
	rootCmd.AddCommand(newActiveCmd())
	rootCmd.AddCommand(newScreenshotCmd())
	rootCmd.AddCommand(newSeeCmd())
	rootCmd.AddCommand(newClipboardCmd())
	rootCmd.AddCommand(newWindowsCmd())
	rootCmd.AddCommand(newAppsCmd())
	rootCmd.AddCommand(newDumpCmd())
	rootCmd.AddCommand(newHandoffCmd())
	rootCmd.AddCommand(newActCmd())
	rootCmd.AddCommand(newDoctorCmd())
	rootCmd.AddCommand(newVersionCliCmd())

	return rootCmd.Execute()
}

// ExitCode extracts exit code from an error (always 1 for now).
func ExitCode(err error) int {
	return 1
}

func newVersionCliCmd() *cobra.Command {
	return &cobra.Command{
		Use:   "version",
		Short: "Print version",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Printf("macctx-pp-cli %s\n", version)
		},
	}
}

// suggestFlag is a placeholder for flag suggestion support.
var _ = strings.Contains

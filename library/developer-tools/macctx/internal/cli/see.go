// Copyright 2026 hiten-shah. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import "github.com/spf13/cobra"

func newSeeCmd() *cobra.Command {
	var path, mode, app, windowTitle, analyze string
	var screenIndex, timeout int
	var annotate, asJSON bool
	cmd := &cobra.Command{
		Use:   "see",
		Short: "Capture a Peekaboo UI inspection snapshot and return snapshot metadata plus a path to an annotated image when requested.",
		RunE: func(cmd *cobra.Command, args []string) error {
			if err := mustHavePeekaboo(); err != nil {
				return err
			}
			pbArgs := peekabooArgs("see")
			pbArgs = addFlag(pbArgs, "--path", path)
			pbArgs = addFlag(pbArgs, "--mode", mode)
			pbArgs = addFlag(pbArgs, "--app", app)
			pbArgs = addFlag(pbArgs, "--window-title", windowTitle)
			pbArgs = addFlag(pbArgs, "--analyze", analyze)
			if screenIndex >= 0 {
				pbArgs = append(pbArgs, "--screen-index", cmd.Flag("screen-index").Value.String())
			}
			if timeout > 0 {
				pbArgs = append(pbArgs, "--timeout-seconds", cmd.Flag("timeout").Value.String())
			}
			pbArgs = addBoolFlag(pbArgs, "--annotate", annotate)
			pbArgs = addBoolFlag(pbArgs, "--json", asJSON)
			res, err := runPeekaboo(pbArgs...)
			if err != nil {
				return err
			}
			return writePeekabooOutput(res, asJSON)
		},
	}
	cmd.Flags().StringVar(&path, "path", "", "output screenshot path")
	cmd.Flags().StringVar(&mode, "mode", "frontmost", "capture mode: screen, window, frontmost")
	cmd.Flags().StringVar(&app, "app", "", "target application name or special value")
	cmd.Flags().StringVar(&windowTitle, "window-title", "", "target window title")
	cmd.Flags().StringVar(&analyze, "analyze", "", "optional AI analysis prompt")
	cmd.Flags().IntVar(&screenIndex, "screen-index", -1, "screen index for screen captures")
	cmd.Flags().IntVar(&timeout, "timeout", 0, "timeout in seconds")
	cmd.Flags().BoolVar(&annotate, "annotate", false, "generate annotated screenshot")
	cmd.Flags().BoolVarP(&asJSON, "json", "j", false, "emit Peekaboo JSON")
	return cmd
}
